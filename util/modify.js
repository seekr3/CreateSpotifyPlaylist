const
    {
        read,
        write,
    } = require('./fs'),
    {
        sortByDate,
        sortByTrackNumber,
        sortAlphabetically,
    } = require('./sort'),
    error = require('./error')
;

const main = async () => {
    

}

if (require.main = module)
    main().catch(error);
;
